machine: 
  java: 
    version: oraclejdk8

test:
 override:
   - gradle clean build

deployment:
  production:
    branch: master
    heroku:
      appname: prorata-api
  staging:
    branch: integration
    heroku:
      appname: prorata-api-staging

general:
  artifacts:
    - "reports/tests/index.html" # Gradle test results
