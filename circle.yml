machine: 
  java: 
    version: oraclejdk8

test:
 override:
   - gradle clean build

deployment:
  production:
    branch: master
    heroku:
      appname: prorata-api
  staging:
    branch: integration
    heroku:
      appname: prorata-api-staging
      
general:
  build_dir: build

general:
  artifacts:
  - "~/ubuntu/ProRata-API/build/reports/tests/index.html"
#    - "reports/tests/index.html" # Gradle test results
